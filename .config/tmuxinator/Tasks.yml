name: Tasks
root: "~"
attach: false
startup_window: 0

windows:
  - zsh:
  - Neovim:
      - nvim
  - Taskwarrior:
      - tmux set status-bg cyan
      - tmux set status-fg black
      - taskwarrior-tui
